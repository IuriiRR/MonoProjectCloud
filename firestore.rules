rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // MVP: open rules for local development only.
    // TODO: replace with proper auth + per-user access controls.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}


