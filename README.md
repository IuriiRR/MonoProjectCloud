## CloudApi (local dev)

### Firestore emulator

```bash
docker compose up --build
```

UI: `http://localhost:4000`  
Firestore: `localhost:8080`

### Users CRUD API (local cloud function)

See `docs/users_api.md`.

### Deploy to GCP

See `terraform/README.md`.


